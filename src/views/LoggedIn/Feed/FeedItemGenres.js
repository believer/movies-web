import styled from 'styled-components'

const FeedItemGenres = styled.div`
  color: ${props => props.theme.primary};
  font-size: 12px;
  margin-bottom: 5px;
  margin-top: 5px;
`

export default FeedItemGenres
